export type PopoverOptions = {
    el: HTMLElement
    nestedFor?: string
}

export interface AnchorOptions {
    anchor: HTMLElement
    el: HTMLElement
    gap?: number
    offset?: number
    placement?: string
}